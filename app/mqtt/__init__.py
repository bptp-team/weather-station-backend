"""Mosquitto broker integration: client, topics and message handlers.

A second entry point (sibling to HTTP): a handler parses the payload with a
schema and delegates to app/services, exactly as a route would.
"""
